<UserControl x:Class="GBookEdit.WPF.BreakMarkerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GBookEdit.WPF"
             mc:Ignorable="d" d:DesignHeight="50" d:DesignWidth="800"
             Background="#3f000000"
             DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Separator VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="8,0" />
        <TextBlock Text="{Binding Header}" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1" Margin="4" />
        <Separator VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="8,0" Grid.Column="2" />
    </Grid>
</UserControl>
